{
  "name": "goponkotha",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build && mkdir -p ../server/public && cp -r dist/* ../server/public/",
    "server-build": "cd server && npm install && npm run build",
    "start": "cd server && node dist/index.js"
  },
  "dependencies": {},
  "devDependencies": {}
}
